# مشروع: مترجم نصوص (Arabic <-> English) — Python Flask

## ملخص
هذا مشروع بسيط لموقع ترجمة نصوص باستخدام Python + Flask مع واجهة داكنة (Dark Theme). يتم تسليم الكود الكامل مع شرح تشغيل محلي وخطوات رفع على AWS EC2.

---

## الملفات الموجودة
- `app.py` — التطبيق الرئيسي (Flask)
- `templates/index.html` — قالب الواجهة
- `static/style.css` — ستايل داكن
- `requirements.txt` — الحزم المطلوبة
- `README.md` — هذا الملف

---

## متطلبات التشغيل
- Python 3.8+ مثبت
- pip
- إمكانية فتح بورت 5000 (للتجربة) أو 80 عند نشر بـ Nginx

---

## تشغيل محلي (خطوات سريعة)
1. افتح التيرمنال وانتقل لمجلد المشروع:
   ```bash
   cd ~/translator

# مشروع: مترجم نصوص (Arabic <-> English) — Python Flask

## ملخص
هذا مشروع بسيط لموقع ترجمة نصوص باستخدام Python + Flask مع واجهة داكنة (Dark Theme). يتم تسليم الكود الكامل مع شرح تشغيل محلي وخطوات رفع على AWS EC2.

---

## الملفات الموجودة
- `app.py` — التطبيق الرئيسي (Flask)
- `templates/index.html` — قالب الواجهة
- `static/style.css` — ستايل داكن
- `requirements.txt` — الحزم المطلوبة
- `README.md` — هذا الملف

---

## متطلبات التشغيل
- Python 3.8+ مثبت
- pip
- إمكانية فتح بورت 5000 (للتجربة) أو 80 عند نشر بـ Nginx

---

## تشغيل محلي (خطوات سريعة)
1. افتح التيرمنال وانتقل لمجلد المشروع:
   ```bash
   cd
 ~/translator

ما ستستلمه مني (تسليم)

الكود المصدر (app.py, templates/, static/, requirements.txt, README.md) — كما أعلاه.

تعليمات تشغيل كاملة لتشغيل محليًا وعلى AWS EC2.

(اختياري بتكلفة إضافية) أقدّم ملف systemd وملف Nginx جاهز للتفعيل وتعديل اسم الدومين.

